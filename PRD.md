# MiniQuickCut 产品需求文档

版本：1.0 MVP  
状态：已确认并进入实现  
更新日期：2026-07-10

## 1. 产品概述

MiniQuickCut 是一个模板驱动的轻量相册 Vlog 生成器。用户不需要理解时间线、关键帧或转场，只需选择照片、画面比例和模板，即可得到一条约 30 秒的带音乐 MP4。

产品不是网页剪辑器。它通过少量安全运镜、交叉淡化、模板参数和确定性随机数，让同一组照片快速得到多个自然但可控的版本。

## 2. 产品目标

- 用户在 3 分钟内完成从选图到首次预览。
- 10–20 张照片在普通设备上可流畅预览。
- 不上传照片，降低隐私顾虑和服务成本。
- 用户只需理解“选照片、选风格、看结果”三件事。
- 最终文件为兼容性良好的 H.264/AAC MP4。

## 3. 非目标

MVP 不实现：视频素材、字幕文字、时间线编辑、自定义单张时长、逐张运镜编辑、滤镜面板、自定义音乐、账户、云端保存、分享链接、1080p/4K、AI 内容分析和鼓点检测。

## 4. 目标用户

- 希望把旅行、聚会、亲子或日常照片快速做成短片的普通用户。
- 不熟悉专业剪辑工具，且不愿意上传私人照片的用户。
- 使用普通笔记本、办公电脑或手机浏览器的用户。

## 5. 核心用户旅程

### 5.1 选择照片

- 用户可点击选择或拖入照片。
- 接受 JPEG、PNG、WebP。
- 最少 10 张，最多 20 张。
- 照片以带序号的缩略图网格展示。
- 支持继续添加、删除、单张替换和拖动排序。
- 不支持格式、超出数量和读取失败时提供明确反馈。
- 进入下一步前显示数量进度。

### 5.2 选择画幅与模板

画幅：

- 16:9 横屏，导出 1280×720
- 9:16 竖屏，导出 720×1280
- 4:3 横屏，导出 960×720
- 3:4 竖屏，导出 720×960

模板：

| 模板 | 视觉性格 | 镜头规则 | 转场 | 音乐 |
| --- | --- | --- | --- | --- |
| 清新漫游 | 明亮自然 | 缓慢推进、轻微上移 | 0.65 秒交叉淡化 | Interstellar Space |
| 温柔回忆 | 柔和舒缓 | 缓慢拉远、左右漂移 | 0.9 秒交叉淡化 | Tender Moment |
| 活力瞬间 | 轻快有节奏 | 小幅推进、方向切换更频繁 | 0.35 秒交叉淡化 | Happy Clappy |

模板卡片展示风格名、简短说明、音乐名和视觉示意。首版不单独开放选歌，避免模板与音乐节奏不匹配。

### 5.3 生成与预览

- 用户点击“一键生成”进入结果页。
- 预览使用与导出完全相同的时间线和绘制函数。
- 播放、暂停、拖动进度和重新播放均可用。
- “换一种感觉”生成新的随机种子并立即得到新版本。
- 重新生成不改变照片顺序、比例或模板。
- 用户可返回模板页或照片页继续修改。

### 5.4 下载 MP4

- 点击后检测 H.264 编码能力。
- 支持时开始本地离线逐帧渲染。
- 显示 0–100% 进度，并说明照片不会上传。
- 导出 720p、24fps、约 30 秒的 H.264/AAC MP4。
- 导出完成后自动触发下载。
- 失败时保留项目和预览，并提供可理解的原因。

## 6. 时间线规则

- 作品固定 30 秒。
- 每张照片至少出现一次，顺序与素材区一致。
- 基础镜头时长为 `30 / 照片数`。
- 每个镜头允许模板控制的微小速度变化，但总时长保持 30 秒。
- 相邻镜头在尾部交叉淡化，转场包含在总时长内。
- 同一组照片、模板、比例和 seed 必须产生完全一致的画面。

## 7. 防黑边规则

每一帧先按 cover 规则计算基础缩放：

`baseScale = max(canvasWidth / imageWidth, canvasHeight / imageHeight)`

再在基础比例上增加 5%–12% 运镜安全余量。随机平移不得超过缩放后图像超出画布的范围；任意时刻四个画布边缘都必须被照片覆盖。

MVP 镜头动作只有：缓慢放大、缓慢缩小、轻微上移、轻微左右移动。转场只有直接切换和交叉淡化。

## 8. 性能要求

- 预览最长边约 720 像素，24fps。
- 导出只保持当前与下一张照片处于绘制热路径。
- 编码逐帧等待背压，不累积未编码帧。
- 视频帧写入后立即释放底层资源。
- 最终 MP4 预计低于 100MB，允许使用内存缓冲目标。
- 页面主交互在导出外不应因照片数量增加而明显卡顿。

## 9. 隐私与数据

- 照片通过浏览器 `File` 和对象 URL 使用。
- 不上传照片，不保存项目，不写入数据库。
- 刷新或关闭页面后当前项目丢失。
- 内置音乐是唯一由站点加载的媒体素材。

## 10. 可用性与无障碍

- 所有主要按钮有明确文本，不依赖纯图标表达。
- 键盘可操作照片删除、替换、步骤切换和播放器。
- 焦点样式清晰。
- 尊重 `prefers-reduced-motion`，界面动画降级；成片本身仍按模板播放。
- 错误和导出状态使用文字与视觉共同表达。

## 11. 验收标准

- 可加入、删除、替换、排序 10–20 张照片。
- 四种比例均无黑边，预览与导出构图一致。
- 三个模板能产生肉眼可见但克制的差异。
- 同一 seed 重复渲染结果一致。
- 重新生成不改变照片顺序。
- 支持的浏览器能下载包含音乐的 MP4。
- 导出错误不会导致照片列表或模板选择丢失。
- 桌面和手机宽度下均可完成全流程。

## 12. 素材许可

下列音频由 John Bartmann 创作，并在 Wikimedia Commons / Free Music Archive 标注为 CC0 1.0：

- [Interstellar Space](https://commons.wikimedia.org/wiki/File:John_Bartmann_-_12_-_Interstellar_Space.ogg)
- [Tender Moment](https://commons.wikimedia.org/wiki/File:John_Bartmann_-_19_-_Tender_Moment.ogg)
- [Happy Clappy](https://commons.wikimedia.org/wiki/File:John_Bartmann_-_09_-_Happy_Clappy.ogg)
- [CC0 1.0 法律文本](https://creativecommons.org/publicdomain/zero/1.0/)

项目保留曲名、作者和来源记录，即使 CC0 不要求署名。若未来替换音乐，每一首都必须单独记录下载地址、作者、许可和核验日期。

## 13. 后续候选

- P1：HEIC 转换、会话恢复、同模板多首音乐、1080p 可选。
- P2：片头文字、日期地点、照片智能排序、服务端导出兜底。
- P3：分享链接、项目账户和轻量单镜头调整。

